import React from "react";
import CartTable from "../components/CartTable"

function Cart() {
    return (
        <div className="cart">
            <CartTable />
        </div>
    );
};

export default Cart;