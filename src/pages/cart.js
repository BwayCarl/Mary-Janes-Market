import React from "react";
import CartTable from "../components/CartTable"

function Cart() {
    return (
        <cart className="cart">
            <CartTable />
        </cart>
    );
};

export default Cart;